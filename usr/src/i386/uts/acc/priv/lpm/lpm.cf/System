#ident	"@(#)kern-i386:acc/priv/lpm/lpm.cf/System	1.3"
#ident	"$Header: $"

# lpm and sum are alternative, mutually exclusive privilege
# mechanisms.  Configure *exactly* *one* of them.  (i.e., if you
# change the second field here, set the second field in sum to be the
# opposite.)

$version 2
lpm	N	0	0	0	0	0	0	0	0	-1
