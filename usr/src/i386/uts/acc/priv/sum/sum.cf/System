#ident	"@(#)kern-i386:acc/priv/sum/sum.cf/System	1.3"
#ident	"$Header: $"

# sum and lpm are alternative, mutually exclusive privilege
# mechanisms.  Configure *exactly* *one* of them.  (i.e., if you
# change the second field here, set the second field in lpm to be the
# opposite.)

$version 2
sum	Y	0	0	0	0	0	0	0	0	-1
