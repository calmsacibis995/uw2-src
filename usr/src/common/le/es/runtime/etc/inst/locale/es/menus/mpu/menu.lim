.pageno
Página %d de %d
.ul
Instalación de Processor Upgrade
.ll
.lr
.top



El par de número de serie y clave de activación 
     `cat /tmp/mpu/KEYPAIR`
se instaló satisfactoriamente.


No obstante, se encontró el siguiente mensaje de error al definir 
el nuevo límite del PROCESADOR:

      `cat /tmp/mpu/lim.errmsg | grep -v "keyadm" | cut -c2-`


Por favor, compruebe el archivo /etc/config/licensekeys.
.form
ENTER
.selection
Pulse <Intro> para continuar.
.end
