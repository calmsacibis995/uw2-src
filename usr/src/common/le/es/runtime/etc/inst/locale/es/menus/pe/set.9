.bottom
Pulse <Intro> para continuar:
#.form
#ENTER
.pageno
Página %d de %d
.ul
Instalación de conjuntos
.hhelp_ban
Ayuda correspondiente a ayuda y a cómo utilizar este programa
.helpbanner
Ayuda correspondiente a instalación de conjuntos
.ur
Resultados de la instalación
.ll
#Please make a selection.  Press F1 for Help.
.lr
F1=Ayuda
.help
Para obtener instrucciones detalladas, pulse <F1> (o '?') en cualquier
momento.

Las pantallas anteriores mostraban el estado de la instalación para
todos los paquetes seleccionados en este conjunto.  

Hallará más detalles sobre la instalación que ha fallado en el correo
electrónico remitido al identificador de entrada "raíz".
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && echo "Los archivos de configuración del sistema no se combinaron para los\nsiguientes paquetes:"`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && echo "    "${MRGFAILLIST}`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && cat $MRGFAILMSG`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && cat $MRGFAILFILES`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && echo "Archivos de configuración del sistema no se combinaron para:"`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && echo "    "${NOMRGLIST}`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && cat $NOMRGMSG`
.helpinst
ESC=Salir de Ayuda  F1=Instrucciones  1=AvPág  2=RePág
.hhelpinst
ESC=Salir de instrucciones  1=AvPág  2=RePág
.working
Operando...
.top
