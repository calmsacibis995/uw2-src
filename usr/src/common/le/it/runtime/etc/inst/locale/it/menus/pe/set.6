.bottom
Premere <Invio> per continuare:
#.form
#ENTER
.pageno
Pagina %d di %d
.ul
Installazione dei set
.hhelp_ban
Guida sulla guida e sull'uso di questo programma
.helpbanner
Guida sull'installazione dei set
.ur
Risultati dell'installazione
.ll
#Please make a selection.  Press F1 for Help.
.lr
F1=Guida
.help
Per istruzioni dettagliate, premere 'F1' (o '?') in qualsiasi momento.

Questo schermo mostra lo stato dell'installazione per tutti i pacchetti
selezionati in questo set. Non si vedranno elencati in questo schermo i
nomi dei pacchetti non selezionati per l'installazione.

Se lo stato, che si trova nella colonna di sinistra, è "Riuscito",
l'installazione di quel pacchetto ha avuto esito positivo. Se lo stato
è "Fallito", l'installazione di quel pacchetto ha avuto esito negativo.

I dettagli sulle installazioni fallite si trovano nei messaggi di posta
elettronica inviati all'utente "root".
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && echo "Combinazione dei file di configurazione del sistema non riuscita per:"`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && echo "    "${MRGFAILLIST}`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && cat $MRGFAILMSG`
`[ "${MRGFAILLIST}" ] && echo " "`
`[ "${MRGFAILLIST}" ] && cat $MRGFAILFILES`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && echo "File di configurazione di sistema non combinati per i seguenti pacchetti:"`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && echo "    "${NOMRGLIST}`
`[ "${NOMRGLIST}" ] && echo " "`
`[ "${NOMRGLIST}" ] && cat $NOMRGMSG`
.helpinst
ESC=Fine guida  F1=Istruzioni  1=Avanti  2=Indietro
.hhelpinst
ESC=Fine istruzioni  1=Avanti  2=Indietro
.working
Attendere...
.top
È stata richiesta l'installazione dei seguenti pacchetti di questo set.
Lo stato dell'installazione di ciascuno di questi pacchetti si trova
alla sinistra del nome.

        Stato           Nome pacchetto
        -----------     --------------------------------
