.pageno
Pagina %d di %d
.ul
Installazione di Processor Upgrade
.ll
.lr
.top



La coppia numero di serie/chiave di attivazione
     `cat /tmp/mpu/KEYPAIR`
è stata installata.


Tuttavia, si è presentato il seguente messaggio di errore durante
l'impostazione del nuovo limite PROCESSOR:

      `cat /tmp/mpu/lim.errmsg | grep -v "keyadm" | cut -c2-`


Verificare il file /etc/config/licensekeys.
.form
ENTER
.selection
Premere <Invio> per continuare.
.end
