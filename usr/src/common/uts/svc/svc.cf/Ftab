#ident	"@(#)kern:svc/svc.cf/Ftab	1.2"
#ident	"$Header: $"

# Function table for routines to be called after the root filesystem
# is mounted, but before the "init" process has been spawned.
#
#  [entry suffix]  [func table name]  [return type]
	postroot    postroot_funcs	void
